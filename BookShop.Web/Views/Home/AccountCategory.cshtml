@using Microsoft.AspNet.Identity;
@{
    ViewBag.Title = "Cá nhân";

}
<div class="sub-cate">
    <div class="top-nav rsidebar span_1_of_left">
        <h3 class="cate">CÁ NHÂN</h3>
        @using (Html.BeginForm("LogOut", "Account", FormMethod.Post, new { @id = "frmLogout" }))
        {
            if (Request.IsAuthenticated)
            {
                var userDetailUrl = "/thong-tin-tai-khoan-" + User.Identity.GetUserId() + ".html";
                var orderHistoryUrl = "/lich-su-mua-hang-" + User.Identity.GetUserId() + ".html";
                <ul class="menu">

                    <li class="subitem3"><a href="@userDetailUrl">THÔNG TIN CÁ NHÂN</a></li>

                    <li class="subitem3"><a href="@orderHistoryUrl">LỊCH SỬ MUA HÀNG</a></li>


                </ul>
            }

        }
    </div>
    <!--initiate accordion-->
    @*<script type="text/javascript">
          $(function () {
            var menu_ul = $('.menu > li > ul'),
                   menu_a = $('.menu > li > a');
            menu_ul.hide();
            menu_a.click(function (e) {
              e.preventDefault();
              if (!$(this).hasClass('active')) {
                menu_a.removeClass('active');
                menu_ul.filter(':visible').slideUp('normal');
                $(this).addClass('active').next().stop(true, true).slideDown('normal');
              } else {
                $(this).removeClass('active');
                $(this).next().stop(true, true).slideUp('normal');
              }
            });

          });
        </script>*@
    <div class="chain-grid menu-chain">
        <a href="single.html"><img class="img-responsive chain" src="/Assets/client/images/wat.jpg" alt="" /></a>

    </div>
</div>